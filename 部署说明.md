# 部署说明 (QMS 演示 / 独立环境)

适用于**无 MES 实时数据**的环境：通过仓库内置的 MES 种子，启动后即可看到完整数据
（供应商、来料异常、来料批次、完工检验、物料追溯）。已接入真实 MES 则跳过第 3 步。

## 1. 拉取代码

后端：

```bash
git clone -b main git@github.com:V-fishing/kangli-backend-v2.git QMS-backend
cd QMS-backend
```

前端（含子模块 `qms-web-new`）：

```bash
git clone -b main https://github.com/V-fishing/kangliQMS.git konliQMS
cd konliQMS
git submodule update --init qms-web-new
cd ..
```

## 2. 数据库与配置

- 准备 PostgreSQL 14+，建库 `qms`、用户 `qms`。
- 后端数据源 / JWT 在 `qms-bootstrap/src/main/resources/application-dev.yml` 配置；
  推荐用环境变量注入：`QMS_DB_HOST` / `QMS_DB_PORT` / `QMS_DB_NAME` / `QMS_DB_USER` /
  `QMS_DB_PASSWORD` / `QMS_JWT_SECRET`。
- MES 宽表 schema 默认 `qms`（可用 `QMS_MES_SCHEMA` 覆盖）。

## 3. 载入 MES 种子（无 MES 环境必须）

```powershell
# Windows (PowerShell)
cd QMS-backend\db\mes-seed
$env:QMS_DB_PASSWORD = '密码'
.\load_mes_seed.ps1            # 重装/重置: .\load_mes_seed.ps1 -Reset
```

```bash
# Linux / macOS
cd QMS-backend/db/mes-seed
QMS_DB_PASSWORD='密码' ./load_mes_seed.sh     # 重置: 加 --reset
```

脚本先建 `qms` schema，再解压灌入来料 / 完工 / 物料绑定三表（gzip 后约 12MB）。
种子细节见同目录 `README.md`。

## 4. 启动后端

```bash
# Windows
mvnw.cmd -pl qms-bootstrap -am package -DskipTests
# Linux / macOS
./mvnw -pl qms-bootstrap -am package -DskipTests
# 或直接运行已构建的 jar
java -jar qms-bootstrap/target/qms-bootstrap.jar
```

应用启动完成后，`MesDataSyncJob` 在 `ApplicationReadyEvent` 时自动从 `qms.*`
同步生成 `ops` 的供应商 / 来料异常 / 来料批次数据（测试 profile 下跳过同步）。

## 5. 前端（可选）

```bash
cd konliQMS/qms-web-new
pnpm install && pnpm dev      # 按实际包管理器调整
```

前端供应商编号显示真实编码（如 `MZ_03.030`），统一信用代码为空时显示「—」。

## 6. 验证

- 供应商列表 / 详情：编号 = 真实码，信用代码 = 「—」。
- 来料异常、来料批次、完工检验均有数据。

## 备注

- 种子数据截至 2026-08-06；重新生成最新快照见 `db/mes-seed/README.md`。
- 已接真实 MES 时**无需**载入种子，由 MES 实时灌入 `qms.*` 即可。
