-- ============================================================
-- V211 体系管理: 不良事件补充负责人(owner)独立列。
-- 原 handle() 误将负责人写入 remark, 导致备注被覆盖且无独立字段。
-- 新增 owner 列承载"处理负责人", remark 回归纯备注语义。幂等。
-- ============================================================

ALTER TABLE ops.qms_adverse_event
  ADD COLUMN IF NOT EXISTS owner VARCHAR(80);  -- 处理负责人
